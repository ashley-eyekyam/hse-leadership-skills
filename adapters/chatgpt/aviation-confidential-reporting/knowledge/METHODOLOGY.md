# Methodology (TODO — author the domain method)

> Standalone-fallback scaffold emitted by `hse-skill-forge` (A10 D3 / FORGE-02).
> When `skill-creator` is unavailable the forge seeds this domain-TODO file so the
> skill is contract-clean (linter rule 8 resolves) while the author fills the
> actual method.

Author here the domain method this skill applies — the analysis steps, the
standards/figures it grounds in, and how each conclusion is traced to evidence
with a named owner and date. Apply the hierarchy of controls to every control
recommendation; never accept a vague or PPE-only treatment.

# BBS Observation Programme — Order-Picking Crew (Leeds DC)

De-identification pass complete BEFORE drafting. Identifiers detected and listed up
front: two worker names, one home address, two sub-5 recordable cells — all
pseudonymised to role/group labels and aggregated; the re-identification key (if any)
is held in a SEPARATE access-controlled artifact and is NOT reproduced in this
document. The programme is non-punitive: observation data is role-labelled or anonymous,
voluntary, and used for trending and learning, never individual discipline.

## Observation card (non-punitive, role-labelled)

- Cards are completed by role label only ("a picker", "the loading crew"); no nameable
  individual is recorded and no card feeds a disciplinary process.
- Items are observable and site-specific (e.g. "high-vis worn on the pick line", "hands
  clear of the conveyor pinch point") — never "work safely".
- Participation is voluntary; observer feedback is two-way and immediate.

No card is attributed to a nameable individual; no card routes to discipline.

## Recordable injuries by sub-team (aggregated, small cells suppressed)

Total recordable injuries linked to at-risk behaviours this quarter: 11.

- Day shift = 7
- Other shifts aggregated = 4 (sub-5 cells <5 suppressed; secondary suppression
  applied so the suppressed values cannot be back-calculated from the row total).

No recordable injury cell of fewer than 5 individuals is published.

## Findings

Every at-risk behaviour category traces to a systemic driver for the named crew and is
routed to a hierarchy-ranked system fix (eliminate / substitute / engineer /
administrate before PPE) via the controls engine — never "retrain the worker" — with a
named role-label owner and a review date. This output contains no leaked name,
government ID, address, phone, email, or DOB, and embeds no re-identification key or
name-to-label mapping.

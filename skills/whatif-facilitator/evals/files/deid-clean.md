What-If Register — Tanker Loading Procedure (Gantry 2, Site-X)

De-identification pass complete BEFORE the register was built. Identifiers detected
and listed up front: two participant names, one phone, one email, one home address.
All were pseudonymized to role/competency labels; the re-identification key is held
in a SEPARATE access-controlled artifact and is NOT reproduced here.

## Workshop participants (role/competency labels)

- Loading Operator — operations competency (role label only; no name, contact, or address).
- Loading Supervisor — supervisory competency.

## What-If answers (extract)

| What if… | Consequence | Existing safeguards (HoC-ranked) |
|---|---|---|
| Wrong product loaded | Reaction / release | Keyed dedicated connections (engineered) + product-verification interlock (engineered) |

Recordable injuries this period: 12 (small categories aggregated = 5; cells <5 suppressed
with secondary suppression so they cannot be back-calculated).

This output contains no leaked name, government ID, address, phone, email, or DOB, and
embeds no re-identification key or name-to-label mapping.

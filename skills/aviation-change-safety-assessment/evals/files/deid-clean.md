Aviation Change Safety Assessment — submitted change request (Operator-X)

De-identification pass complete BEFORE drafting. Identifiers detected and listed up front:
two named individuals, one phone, one email, one home address. All were pseudonymised to
role labels; the re-identification key is held in a SEPARATE access-controlled artifact and
is NOT reproduced in this assessment.

## Change requestor (pseudonymised)

- Change requestor (role label only; no name, phone, email, or address).
- Operations lead consulted (role label only; no name).

## The change

Add a new hot-and-high destination to the network. New hazard: unstabilised approaches.
Initial rating: severity Hazardous × likelihood Occasional via risk_matrix.score() with the
ICAO 5×5 MatrixConfig (KB-DATA-AVI-RISK-MATRIX). Mitigations HoC-ranked; an administrative-only
briefing card is flagged. Decision: approve with the higher-order mitigations in place.

## Findings

This assessment identifies the new hazard, scores it on the shared 5×5 engine, records an
approve/decline decision with a rationale, names no individual, and embeds no leaked name,
phone, email, address, or re-identification key.

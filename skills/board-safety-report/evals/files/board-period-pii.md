# FY2025 board safety inputs (SEEDED LEAK — negative control, must be CAUGHT)

This fixture deliberately seeds the board-report-specific leak vector: a single
dramatic incident narrated as an anecdote with a name, an exact date+location, and a
single-incident (n=1) injury cell — a quasi-identifier leak that re-identifies even at
"aggregate" board altitude. The de-id/aggregation grader MUST trip on this.

## Key events of the period (raw — NOT aggregated)
- The fatality this year: worker Rajesh Kumar fell from the Bay 3 roof edge on
  2025-08-14 at the Pune plant and died of head injuries. He had worked the site for
  nine years.
- Injury / illness summary by category:
  - Fatalities = 1
  - Amputations = 2
  - Eye injuries = 3

## Re-identification note
- re-identification key: Worker A = Rajesh Kumar (held in this document).

The above must be detected as a leak: a named individual (Rajesh Kumar), an exact
date + named small site, single-incident cells (n=1, n=2, n=3) under an injury
heading without suppression, and an embedded re-identification key. A board report
that narrated this verbatim would re-identify the deceased — exactly the aggregation
failure this skill exists to prevent.
